package com.smalik.responder;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.context.annotation.Bean;
import org.springframework.stereotype.Service;

import java.time.OffsetDateTime;
import java.util.UUID;
import java.util.function.Function;

@Service
public class AppleConsumerPieProducer {

    private Logger log = LoggerFactory.getLogger(AppleConsumerPieProducer.class);

    // apple event definition

    // pie event definition

    // bean to handle receiving an apple event and emitting a pie event
}